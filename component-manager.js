/**
 * Main Entrypoint
 */ 

class ComponenetManager {
  // maybe contains= strip that equal sign use shellparsing. JS/ZSH/BASH not needed with NodeJS process.argv handels that
  
  '--start': ()=>{}
}

for (let key of Object.keys(ComponentManager.prototype)) {
  process.argv.contains(key) && 
}

export { }
